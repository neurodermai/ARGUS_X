# ARGUS-X Backend — Production Dependencies (ML Enabled)
# Uses CPU-only PyTorch via --extra-index-url to minimize image size (~2.5GB vs ~4GB)
# Full ML pipeline: ONNX classifier + SBERT embeddings + tokenizer

--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# Supabase
supabase>=2.3.0
postgrest>=0.13.0

# LLM
litellm>=1.16.0

# ML / ONNX (core classifier — DistilBERT inference)
onnxruntime>=1.20.0
transformers>=4.36.0
tokenizers>=0.15.0
numpy>=1.26.0

# NLP / Security (SBERT embeddings for clustering + mutation + auditor)
sentence-transformers>=2.2.2
scikit-learn>=1.3.0

# Model hosting
huggingface-hub>=0.20.0

# Monitoring
sentry-sdk[fastapi]>=1.39.0

# Utilities
httpx>=0.25.0
aiofiles>=23.2.0
python-multipart>=0.0.6
