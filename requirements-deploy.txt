# ARGUS-X Backend — Production Deployment Dependencies
# Includes ONNX ML inference (no PyTorch) — enables ML classifier in production
# Excludes sentence-transformers (pulls PyTorch ~1.5GB) — those features have fallbacks

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# Supabase
supabase>=2.3.0
postgrest>=0.13.0

# LLM
litellm>=1.16.0

# Utilities
httpx>=0.25.0
aiofiles>=23.2.0
python-multipart>=0.0.6

# Session persistence
redis>=5.0.0

# ML / ONNX inference (no PyTorch dependency)
onnxruntime>=1.20.0
transformers>=4.36.0
tokenizers>=0.15.0
numpy>=1.26.0
scikit-learn>=1.3.0
huggingface-hub>=0.20.0

# Monitoring + Security
sentry-sdk[fastapi]>=1.39.0
slowapi>=0.1.9
