# ARGUS-X Backend — Production Deployment Dependencies (pinned)
# Includes ONNX ML inference (no PyTorch) — enables ML classifier in production
# Excludes sentence-transformers (pulls PyTorch ~1.5GB) — those features have fallbacks
#
# IMPORTANT: All shared dependencies MUST use identical versions as requirements.txt

fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic==2.11.3
python-dotenv==1.1.0

# Supabase (postgrest resolved automatically by supabase dependency)
supabase==2.15.2

# LLM
litellm==1.73.6

# Utilities
httpx==0.28.1
aiofiles==24.1.0
python-multipart==0.0.20

# Session persistence
redis==5.3.0

# ML / ONNX inference (no PyTorch dependency)
onnxruntime==1.22.0
transformers==4.52.4
tokenizers==0.21.1
numpy==2.2.6
scikit-learn==1.6.1
huggingface-hub==0.32.4

# Monitoring + Security
sentry-sdk[fastapi]==2.23.1
slowapi>=0.1.9
python-jose>=3.3.0
