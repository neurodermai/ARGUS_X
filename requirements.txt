# ARGUS-X Backend Dependencies (pinned for reproducible builds)
# NOTE: sentence-transformers is dev-only (pulls PyTorch ~1.5GB).
#       Production uses requirements-deploy.txt which excludes it.
fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic==2.11.3
python-dotenv==1.1.0

# Supabase
supabase==2.15.2
postgrest==0.19.4

# LLM
litellm==1.73.6

# ML / ONNX
onnxruntime==1.22.0
transformers==4.52.4
tokenizers==0.21.1
numpy==2.2.6

# NLP / Security (dev-only — heavy PyTorch dependency)
sentence-transformers==4.1.0
scikit-learn==1.6.1

# Monitoring + Security
sentry-sdk[fastapi]==2.23.1
slowapi==0.1.9

# Utilities
httpx==0.28.1
aiofiles==24.1.0
python-multipart==0.0.20
huggingface-hub==0.32.4
redis==5.3.0
